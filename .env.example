# Optional: raises GitHub API rate limits for local development.
GITHUB_TOKEN=

# Optional: when set, the analyzer adds a small AI-assisted suggestion block.
# The app works without these values.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
